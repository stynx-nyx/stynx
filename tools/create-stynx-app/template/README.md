# **APP_NAME**

Standalone Angular starter for STYNX web packages.

## Start

```sh
pnpm install
pnpm start
```

The STYNX defaults live in `src/app/app.config.ts`. Replace the OIDC and API URLs there with the environment for your tenant.
