[**@stynx-nyx/sdk**](../index.md)

---

[@stynx-nyx/sdk](../index.md) / $TenantSummary

# Variable: $TenantSummary

> `const` **$TenantSummary**: `object`

Defined in: [packages-web/sdk/src/generated/schemas/$TenantSummary.ts:5](https://github.com/aarusso-nyx/stynx/blob/f54a7f51a22215c0ee06c6f06d8420d5b798763d/packages-web/sdk/src/generated/schemas/$TenantSummary.ts#L5)

## Type Declaration

### properties

> `readonly` **properties**: `object`

#### properties.createdAt

> `readonly` **createdAt**: `object`

#### properties.createdAt.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.createdAt.type

> `readonly` **type**: `"string"` = `'string'`

#### properties.id

> `readonly` **id**: `object`

#### properties.id.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.id.type

> `readonly` **type**: `"string"` = `'string'`

#### properties.isActive

> `readonly` **isActive**: `object`

#### properties.isActive.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.isActive.type

> `readonly` **type**: `"boolean"` = `'boolean'`

#### properties.name

> `readonly` **name**: `object`

#### properties.name.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.name.type

> `readonly` **type**: `"string"` = `'string'`

#### properties.slug

> `readonly` **slug**: `object`

#### properties.slug.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.slug.type

> `readonly` **type**: `"string"` = `'string'`

#### properties.state

> `readonly` **state**: `object`

#### properties.state.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.state.type

> `readonly` **type**: `"TenantState"` = `'TenantState'`

#### properties.updatedAt

> `readonly` **updatedAt**: `object`

#### properties.updatedAt.isRequired

> `readonly` **isRequired**: `true` = `true`

#### properties.updatedAt.type

> `readonly` **type**: `"string"` = `'string'`
