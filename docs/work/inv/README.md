# Inventory Work Artifacts

This directory stores repository inventory and gap-analysis outputs that are evidence for cleanup, migration, and rationalization work.

Keep generated or scratch inventory artifacts under `docs/work/inv/**` so the repository root does not accumulate one-shot audit output. Preserve source report content when relocating files into this directory.
