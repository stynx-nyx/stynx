# FE-WAVE-B Report

| Workstream | Commit | Validation excerpt | Notes |
| ---------- | ------ | ------------------ | ----- |
| B.1 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam build`, `lint`, `typecheck`, `test` exit 0; `pnpm -r --filter './packages-web/*' lint`, `build`, `test` exit 0; `pnpm test:evidence` wrote coverage/test-evidence.json; `pnpm test:matrix --no-color --coverage` exit 0 | Scaffolded `@stynx-web/angular-iam@0.1.0` APF package and matrix thresholds; coverage remains `0` until B.9 tests. |
| B.2 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, `test` exit 0; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added `IamApiService`, typed IAM request/response shapes, SDK-backed admin endpoint calls, and signal caches for users/roles/groups. |
| B.3 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, `test` exit 0; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added standalone users admin, user detail, create dialog, and disable confirm components with signal state, typed forms, user role/group assignment, and translated templates. |
| B.4 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, `test` exit 0; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added standalone roles admin, role detail, role create/clone dialog, and permission matrix components with signal state, typed forms, translated templates, and role permission persistence. |
| B.5 | `0b5c152` (workspace scope uncommitted) | `pnpm --filter @stynx-web/angular-iam lint`, `typecheck`, `pnpm -r --filter @stynx-web/angular-iam build`, `test` exit 0; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added standalone groups admin, group detail, create dialog, roles editor, and members editor components with signal state, typed forms, translated templates, and group role/member persistence. |
| B.6 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, `test` exit 0; `pnpm test:evidence` wrote `coverage/test-evidence.json`; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added standalone effective-permissions viewer with signal state, typed search form, translated template keys, SDK-backed refresh, empty/loading/error states, and role/group grant-source chips. |
| B.7 | `0b5c152` (workspace scope uncommitted) | `pnpm --filter @stynx-web/angular-iam lint`, `typecheck`, `pnpm -r --filter @stynx-web/angular-iam build`, `test` exit 0; `pnpm test:evidence` wrote `coverage/test-evidence.json`; `pnpm test:matrix --no-color --coverage` exit 0 with `@stynx-web/angular-iam` coverage `0` pending B.9 | Added `iamRoutes()`, `IAM_ROUTES`, `provideStynxIam({ clientFactory })`, and exported the B.7 route/provider API with permission metadata and `stynxPermissionGuard(...)` guards. |
| B.8 | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam build`, `pnpm --filter @stynx-web/angular-iam lint`, `typecheck`, `pnpm -r --filter @stynx-web/angular-iam test`, catalog parity/no-raw-template checks, and `pnpm test:matrix --no-color --coverage` exit 0; `pnpm i18n:check` and package `test:mutation` absent; root `pnpm lint` red outside B.8 on `test/db/runtime/auth/rls-policies.runtime.spec.ts` unused var | Added `src/i18n/en.json` + `src/i18n/pt-BR.json` with 274 IAM keys, verified all referenced/dynamic keys are covered, and shipped catalogs via ng-packagr assets. |
| B.8 follow-up | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam build` exit 0; `pnpm -r --filter @stynx-web/angular-iam test` exit 0; `pnpm --filter @stynx-web/angular-iam exec node -e "require('@stynx-web/angular-iam/catalogs/en.json'); require('@stynx-web/angular-iam/catalogs/pt-BR.json')"` exit 0; dist manifest check confirms `./catalogs/*.json` exports | Added IAM catalog subpath exports for workspace consumers and normalized generated `dist/package.json` after ng-packagr preserves custom exports. |
| B.9 | `0b5c152` (workspace scope uncommitted) | `pnpm --filter @stynx-web/angular-iam lint`, `typecheck`, `pnpm -r --filter @stynx-web/angular-iam build`, and `test` exit 0; direct `vitest --coverage` exit 0 with lines/statements `91.86`, branches `82.06`, functions `86.58`; `pnpm -r --filter @stynx-web/angular-iam test:mutation` reports no selected package script; `pnpm test:matrix --no-color --coverage` exit 0 using the 2026-05-19T07:58:59.990Z evidence snapshot, which still reports IAM `0` | Added IAM service, route/permission-guard, and per-component TestBed specs under `packages-web/angular-iam/test/`; mutation is not configured for the package. |
| FE-B reference-web mount | `0b5c152` (workspace scope uncommitted) | `pnpm -r --filter @stynx-web/angular-iam build`, `pnpm --filter @stynx/reference-web build`, `test`, and `build:web` exit 0; `curl http://127.0.0.1:3100/admin/users` returned `200 text/html`; `pnpm -r --filter @stynx-web/angular-iam test` and `typecheck` red in B.9-owned `test/components.spec.ts`; `pnpm --filter @stynx/reference-web lint` red on pre-existing OnPush component rule debt only; Playwright browser smoke blocked by macOS `bootstrap_check_in ... Permission denied (1100)` | Mounted `iamRoutes()` under `/admin`, added the Admin shell nav link, wired `provideStynxIam(...)` through the reference SDK client, and merged IAM catalogs into the reference i18n loader. |
| B.9 typecheck repair | `0b5c152` (workspace scope uncommitted) | `pnpm --filter @stynx-web/angular-iam typecheck` exit 0; `pnpm -r --filter @stynx-web/angular-iam test` exit 0 with 2 files / 27 tests passed; `pnpm -r --filter @stynx-web/angular-iam build` exit 0; `git diff --check -- packages-web/angular-iam docs/work/plan/FE-WAVE-B-report.md` exit 0 | Narrowed the component test fake `createGroup` return to `StynxGroup` so `exactOptionalPropertyTypes` accepts mocked groups whose `description` may be `null`. |

## Promotion Summary

Promoted 2026-05-19T15:44:21Z against workspace base `0b5c152` with local, uncommitted FE patches. B.1-B.9 are implemented, `reference/web` mounts `iamRoutes()` under `/admin`, and the parent audit re-ran `pnpm --filter @stynx-web/angular-iam typecheck`, `pnpm -r --filter @stynx-web/angular-iam test`, `pnpm -r --filter @stynx-web/angular-iam build`, `pnpm lint`, `pnpm i18n:check`, `pnpm test:matrix --no-color --coverage`, `CI=true pnpm install --frozen-lockfile --offline --ignore-scripts`, and `git diff --check`; all exited 0. IAM mutation remains intentionally deferred to FE-G by operator decision.
