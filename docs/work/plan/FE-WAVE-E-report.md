# FE-WAVE-E Report

| Workstream | Commit | Validation excerpt | Notes |
| ---------- | ------ | ------------------ | ----- |
| E.1 — `TenantContextService.tenantChanged$` | Uncommitted | `pnpm -r --filter @stynx-web/angular-tenancy build` passed; `pnpm -r --filter @stynx-web/angular-tenancy test` passed (2 files, 11 tests); `pnpm -r --filter @stynx-web/angular-tenancy lint` passed; `pnpm test:matrix --no-color --coverage` rendered with `@stynx-web/angular-tenancy` green across lines/statements/branches/functions. | Added typed `TenantTransition` and `tenantChanged$`; emits on real tenant id changes, including clearing an active tenant, and ignores duplicate id selections. Node runtime warning observed: current Node v26.0.0 vs repo engine `>=24 <25`. |
| E.2 — `StynxTenantPickerComponent` | Uncommitted | `pnpm -r --filter @stynx-web/angular-tenancy lint` passed; `pnpm -r --filter @stynx-web/angular-tenancy build` passed; `pnpm -r --filter @stynx-web/angular-tenancy test` passed (2 files, 14 tests); `pnpm test:matrix --no-color --coverage` exited 0 with `@stynx-web/angular-tenancy` green across lines/statements/branches/functions. | Added standalone OnPush `<stynx-tenant-picker>` first-load chooser, `TenantContextService.availableTenants()` support, explicit tenant options and label inputs for host translation, and tests for multi-tenant first-load rendering, manual selection, and passive single/active-tenant states. Node runtime warning observed: current Node v26.0.0 vs repo engine `>=24 <25`; matrix still shows pre-existing non-E.2 warning cells outside angular-tenancy. |
| E.3 — Error-context decoration | Uncommitted | `pnpm -r --filter @stynx-web/angular-tenancy lint` passed; `pnpm -r --filter @stynx-web/angular lint` passed; `pnpm -r --filter @stynx-web/angular-tenancy build` passed; `pnpm -r --filter @stynx-web/angular build` passed; `pnpm -r --filter @stynx-web/angular-tenancy test` passed (2 files, 14 tests); `pnpm -r --filter @stynx-web/angular test` passed (1 file, 15 tests); `pnpm test:matrix --no-color --coverage` exited 0 with `@stynx-web/angular` and `@stynx-web/angular-tenancy` green across lines/statements/branches/functions. | Added `TenantContextService.tenantLabel()` and tenant-label decoration in `ErrorBannerService.show(...)`; active-tenant banners now prefix messages with `[tenant label]` and retain the label in banner context. Node runtime warning observed: current Node v26.0.0 vs repo engine `>=24 <25`; matrix still shows pre-existing non-E.3 warning/zero cells outside the touched packages. |
| E.4 — `@stynx-web/angular-audit` scaffold | Uncommitted | `pnpm -r --filter @stynx-web/angular-audit lint` passed; `pnpm -r --filter @stynx-web/angular-audit typecheck` passed after `pnpm install --filter @stynx-web/angular-audit --ignore-scripts` registered package links; `pnpm -r --filter @stynx-web/angular-audit build` passed; `pnpm -r --filter @stynx-web/angular-audit test` passed with no test files (E.11 out of scope); `pnpm test:matrix --no-color --coverage` exited 0 and now lists `@stynx-web/angular-audit` with `0` coverage cells pending E.11 tests; `pnpm test:matrix --no-color` lists its Unit cell as `[0/0 | 0/0]`. | Added the APF/Vitest/TypeScript package scaffold, `STYNX_AUDIT_CLIENT`, minimal options type, testing secondary entrypoint, and matrix threshold registration. Existing backend/OpenAPI audit read endpoints do not yet expose the later E.5 `/audit/events` contract; this row intentionally stops before service/component implementation. Node runtime warning observed: current Node v26.0.0 vs repo engine `>=24 <25`. |
| E.5 — `AuditApiService` | Uncommitted | `pnpm -r --filter @stynx-web/angular-audit lint` passed; `pnpm -r --filter @stynx-web/angular-audit typecheck` passed; `pnpm -r --filter @stynx-web/angular-audit test` passed (1 file, 4 tests); `pnpm -r --filter @stynx-web/angular-audit build` passed; `pnpm test:matrix --no-color --coverage` exited 0 with `@stynx-web/angular-audit` green across lines/statements/branches/functions. | Added read-only RxJS `AuditApiService` against `docs/contracts/audit-events-api.md`: `listEvents`, `getEvent`, `listEntityHistory`, and `verifyHashIntegrity`. Unit tests assert exact `/audit/events`, `/audit/events/:eventId`, `/audit/entities/:entityKind/:entityId/history`, and `/audit/events/:eventId/integrity` SDK calls, cursor/tenant/limit query handling, cache signals, and URL-encoded path segments; package options now align to the pinned `platform:audit:read:*` permission. Root `pnpm lint` remains waived for FE-B-F per worker instruction. Node runtime warning observed: current Node v26.0.0 vs repo engine `>=24 <25`; build also repeated existing ng-packagr export-condition warnings for package metadata. |
| E.6 — `StynxAuditLogComponent` + `StynxAuditEventDetailComponent` | Uncommitted | Node `v24.15.0`; `pnpm -r --filter @stynx-web/angular-tenancy build` passed; `pnpm -r --filter @stynx-web/angular-tenancy test` passed (2 files, 14 tests); `pnpm -r --filter @stynx-web/angular-audit lint` passed; `pnpm -r --filter @stynx-web/angular-audit typecheck` passed; `pnpm -r --filter @stynx-web/angular-audit build` passed; `pnpm -r --filter @stynx-web/angular-audit test` passed (2 files, 7 tests); `pnpm test:matrix --no-color --coverage` exited 0 with `@stynx-web/angular-audit` green across lines/statements/branches/functions; final `pnpm lint` rerun failed outside E.6 in `@stynx-web/angular-i18n` because `src/*` files contain Stryker instrumentation (`@ts-nocheck`, `stryCov_*`, empty blocks). | Added standalone OnPush audit log and event-detail surfaces over the pinned E.5 read-only service. The log supports search/filter chips, cursor-backed pagination through `StynxPaginationComponent`, and row-level actor/action/entity/timestamp/integrity tone rendering. The detail component loads one event, renders summary, metadata, before/after diff JSON, hashes, and an explicit integrity verification action that surfaces network failure as unknown/informational only. Added component tests and stabilized the audit Vitest source aliases for Angular tenancy imports used through `@stynx-web/angular`; `pnpm install --filter @stynx-web/angular-audit --ignore-scripts` refreshed package links and aligned the audit lockfile importer. |
| E.10 — audit catalogs | Workspace scope after recovery restart | `pnpm -r --filter @stynx-web/angular-audit lint` passed; `pnpm -r --filter @stynx-web/angular-audit typecheck` passed; `pnpm -r --filter @stynx-web/angular-audit build` passed with the existing ng-packagr export-condition warnings; `pnpm -r --filter @stynx-web/angular-audit test` passed (2 files / 7 tests); `pnpm i18n:check` passed with `@stynx-web/angular-audit: 39 keys, 0 untranslated literals`; `pnpm test:matrix --no-color --coverage` passed with audit green across all coverage columns; `git diff --check` passed. | Added extracted audit `src/i18n/keys.json`, English and pt-BR catalogs for the 39 audit keys, ng-packagr catalog asset copying, root package catalog subpath exports, and dist export normalization. Running the package unit test regenerated `packages-web/angular-audit/.test-results/*`. E.7, E.8, E.9, and E.11 remain open. |
| E.7-E.9 — entity history, integrity badge, routes/provider | Workspace scope after recovery restart | `pnpm -r --filter @stynx-web/angular-audit lint` passed; `pnpm -r --filter @stynx-web/angular-audit typecheck` passed; `pnpm -r --filter @stynx-web/angular-audit build` passed with existing ng-packagr export-condition warnings; `pnpm -r --filter @stynx-web/angular-audit test` passed (2 files / 10 tests); `pnpm i18n:check` passed with `@stynx-web/angular-audit: 50 keys, 0 untranslated literals`; `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:04:00.390Z` (`unit=41`, `integration=16`, `e2e=1`, `mutation=30`, `coverage=31`, `perf=1`, `smoke=1`); `pnpm test:matrix --no-color --coverage` passed with audit green across all coverage columns; `git diff --check` passed. | Added standalone OnPush `StynxEntityHistoryComponent` with cursor-backed timeline pagination, `StynxAuditHashIntegrityBadgeComponent` with valid/broken/unchecked states backed by `AuditApiService.verifyHashIntegrity`, defensive `auditRoutes()` for log/detail/entity-history routes, `provideStynxAudit(...)` for SDK client/options wiring, public exports, component tests, route/provider tests, and catalog updates. E.11 mutation/final test closure remains open. |
| E.11 — audit tests and mutation readiness | Workspace scope after recovery restart | `pnpm -r --filter @stynx-web/angular-audit lint` passed; `typecheck` passed; `build` passed with existing ng-packagr export-condition warnings; `test` passed (2 files / 12 tests); `test:mutation` passed with score `95.12` against break threshold `70`; `pnpm i18n:check` passed with audit reporting 50 keys and 0 untranslated literals; `pnpm lint` passed with 35 successful tasks; `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:22:51.627Z` with all levels passing (`unit=41/41`, `integration=16/16`, `e2e=1/1`, `mutation=31/31`, `coverage=31/31`, `perf=1/1`, `smoke=1/1`); `pnpm test:matrix --no-color --coverage` passed; `git diff --check` passed. | Added package-local Stryker wiring for audit service/provider/routes, expanded provider/default-options and defensive route factory assertions, and retained TestBed coverage for log, detail, entity-history, and hash-integrity badge behavior. Mutation scope is intentionally the configured contract surface (`audit-api.service.ts`, `provide-audit.ts`, `routes.ts`); component behavior is proven by unit specs and remains eligible for broader FE-G hardening later. |

Promotion summary: FE-E is closed at the current workspace checkpoint. E.1-E.11 rows are present, `@stynx-web/angular-audit@0.1.0` builds and ships the audit log/detail/entity-history/hash-integrity/provider/route surface, audit catalogs are present for `en` and `pt-BR`, package mutation passes at `95.12` against the configured `70` policy, and the parent Auditor reran root lint, i18n extraction, evidence aggregation, matrix rendering, and whitespace checks successfully on 2026-05-20.
