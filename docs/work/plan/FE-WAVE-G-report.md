# FE-WAVE-G Report

| Package / workstream | Tests added or audited | Mutation result | Matrix result | Global blockers |
| -------------------- | ---------------------- | --------------- | ------------- | --------------- |
| `@stynx-web/angular-iam` / FE-B IAM mutation and route fan-out | Audited existing TestBed and route coverage: 17 Vitest files / 43 tests covering IAM admin components, route activation/denial, API wrappers, provider/route factory behavior, and admin detail/editor flows. Parent reran `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, and `test`; all passed. | `pnpm -r --filter @stynx-web/angular-iam test:mutation` passed with score `74.77` against break threshold `70` over the configured 5-file mutation set (`effective-permissions.component.ts`, `iam-api.service.ts`, `provide-iam.ts`, `routes.ts`, `users-admin.component.ts`). | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:22:51.627Z`; `pnpm test:matrix --no-color --coverage` passed with IAM green across lines/statements/branches/functions. | None for this slice. Survivor list remains non-empty but below policy threshold; broader FE-G can harden `effective-permissions` and `users-admin` later if desired. |
| `@stynx-web/angular-audit` / FE-E E.11 audit test fan-out | Added provider wiring, default-options, route metadata, and defensive route clone assertions; retained TestBed component coverage for log/detail/entity-history/hash-integrity behavior. Parent reran `pnpm -r --filter @stynx-web/angular-audit lint`, `typecheck`, `build`, and `test`; all passed. | `pnpm -r --filter @stynx-web/angular-audit test:mutation` passed with score `95.12` against break threshold `70` over the configured service/provider/routes mutation set (`audit-api.service.ts`, `provide-audit.ts`, `routes.ts`). | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:22:51.627Z`; `pnpm test:matrix --no-color --coverage` passed with audit green across lines/statements/branches/functions. | None for this slice. Component source mutation is not in the current audit Stryker scope; component behavior is covered by unit specs and can be expanded in later FE-G work if the programme raises the mutation scope. |
| `@stynx-web/angular-sessions` / FE-C sessions adapter/provider/component test fan-out | Added 13 focused tests for `SdkSessionsAdapter`, `STYNX_SESSIONS_CLIENT` / `STYNX_SESSIONS_ADAPTER`, `provideStynxSessions(...)`, provider/input adapter selection, SDK session normalization/current-session derivation, component load/revoke/revoke-others/busy/error behavior. Parent refreshed global gates after worker closure. | `pnpm --filter @stynx-web/angular-sessions stryker` passed with evidence score `98.24561403508771` against break threshold `70` over `active-sessions.component.ts`, `sdk-sessions.adapter.ts`, `provide-sessions.ts`, and `tokens.ts`. | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z`; `pnpm test:matrix --no-color --coverage` passed with sessions green across lines/statements/branches/functions. | None for this slice. This closes FE-C C.9. |
| `@stynx-web/angular-flow` / FE-F flow tests and mutation/router fan-out | Added 6 focused tests for real Router activation and guard denial, dashboard analytics stale-refresh handling, run activity paging/error/empty behavior, fill editor file/signature/reveal behavior, and provider ergonomics. Stryker scope now includes `flow-run-activity.component.ts` and `tokens.ts`; thresholds were not lowered. | `pnpm --filter @stynx-web/angular-flow stryker` passed the configured break threshold with evidence score `65.29492455418381` against break threshold `60` across 12 files / 1050 mutants. | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z`; `pnpm test:matrix --no-color --coverage` passed with flow green across lines/statements/branches/functions. | FE-F remains open because its wave success criterion requires mutation `>= 70 %`, higher than the current package break threshold. |

Current status: FE-G is in progress, not closed. IAM, audit, sessions, and flow package mutation/test fan-out now have current Auditor-verified rows. Remaining FE-G work includes storage/trash/i18n final fan-out, raising `@stynx-web/angular-flow` from repo-green `65.29492455418381` to the FE-F `>= 70 %` wave bar, shared TestBed support where still missing, Playwright fan-out, real-OIDC opt-in, a11y reporting, deletion or relocation of stale per-package e2e smokes, and final threshold review.
