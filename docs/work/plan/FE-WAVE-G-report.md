# FE-WAVE-G Report

| Package / workstream | Tests added or audited | Mutation result | Matrix result | Global blockers |
| -------------------- | ---------------------- | --------------- | ------------- | --------------- |
| `@stynx-web/angular-iam` / FE-B IAM mutation and route fan-out | Audited existing TestBed and route coverage: 17 Vitest files / 43 tests covering IAM admin components, route activation/denial, API wrappers, provider/route factory behavior, and admin detail/editor flows. Parent reran `pnpm -r --filter @stynx-web/angular-iam lint`, `typecheck`, `build`, and `test`; all passed. | `pnpm -r --filter @stynx-web/angular-iam test:mutation` passed with score `74.77` against break threshold `70` over the configured 5-file mutation set (`effective-permissions.component.ts`, `iam-api.service.ts`, `provide-iam.ts`, `routes.ts`, `users-admin.component.ts`). | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:22:51.627Z`; `pnpm test:matrix --no-color --coverage` passed with IAM green across lines/statements/branches/functions. | None for this slice. Survivor list remains non-empty but below policy threshold; broader FE-G can harden `effective-permissions` and `users-admin` later if desired. |
| `@stynx-web/angular-audit` / FE-E E.11 audit test fan-out | Added provider wiring, default-options, route metadata, and defensive route clone assertions; retained TestBed component coverage for log/detail/entity-history/hash-integrity behavior. Parent reran `pnpm -r --filter @stynx-web/angular-audit lint`, `typecheck`, `build`, and `test`; all passed. | `pnpm -r --filter @stynx-web/angular-audit test:mutation` passed with score `95.12` against break threshold `70` over the configured service/provider/routes mutation set (`audit-api.service.ts`, `provide-audit.ts`, `routes.ts`). | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T04:22:51.627Z`; `pnpm test:matrix --no-color --coverage` passed with audit green across lines/statements/branches/functions. | None for this slice. Component source mutation is not in the current audit Stryker scope; component behavior is covered by unit specs and can be expanded in later FE-G work if the programme raises the mutation scope. |
| `@stynx-web/angular-sessions` / FE-C sessions adapter/provider/component test fan-out | Added 13 focused tests for `SdkSessionsAdapter`, `STYNX_SESSIONS_CLIENT` / `STYNX_SESSIONS_ADAPTER`, `provideStynxSessions(...)`, provider/input adapter selection, SDK session normalization/current-session derivation, component load/revoke/revoke-others/busy/error behavior. Parent refreshed global gates after worker closure. | `pnpm --filter @stynx-web/angular-sessions stryker` passed with evidence score `98.24561403508771` against break threshold `70` over `active-sessions.component.ts`, `sdk-sessions.adapter.ts`, `provide-sessions.ts`, and `tokens.ts`. | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z`; `pnpm test:matrix --no-color --coverage` passed with sessions green across lines/statements/branches/functions. | None for this slice. This closes FE-C C.9. |
| `@stynx-web/angular-flow` / FE-F flow tests and mutation/router fan-out | Added 6 focused tests for real Router activation and guard denial, dashboard analytics stale-refresh handling, run activity paging/error/empty behavior, fill editor file/signature/reveal behavior, and provider ergonomics. Stryker scope now includes `flow-run-activity.component.ts` and `tokens.ts`; repo thresholds were not lowered. | `pnpm --filter @stynx-web/angular-flow stryker` passed the configured break threshold with evidence score `65.29492455418381` against break threshold `60` across 12 files / 1050 mutants. | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z`; `pnpm test:matrix --no-color --coverage` passed with flow green across lines/statements/branches/functions. | Accepted by operator decision on 2026-05-20: FE-F mutation acceptance follows the repo-configured threshold, so this row is closed at the accepted evidence snapshot. |
| `@stynx-web/angular-flow` / FE-G package mutation closure | Added focused assertions for exact route path/component/guard metadata, exact FlowApiService endpoint method/path/options contracts, fill editor reveal/text/collection/signature guard branches, and design-dialog defaults/outputs. Files changed: `packages-web/angular-flow/test/components.spec.ts`, `test/flow-api.service.spec.ts`, `test/routes-and-exports.spec.ts`; refreshed `packages-web/angular-flow/.test-results/unit.json`, `unit.junit.xml`, and `mutation.json`. | `pnpm --filter @stynx-web/angular-flow stryker` passed with final score `75.36` (`covered 76.48`) across 12 files / 1050 mutants; killed `569`, survived `175`, no coverage `11`, compile errors `295`; `.test-results` metric score `76.47849462365592`; configured thresholds unchanged at `60/60/60`. | `pnpm test:evidence` regenerated `coverage/test-evidence.json` at `2026-05-20T16:14:28.863Z`; `pnpm test:matrix --no-color --coverage` passed with every package row green, including `@stynx-web/angular-flow` green across lines/statements/branches/functions. | None for this package slice. Required validation also passed: Node `v24.15.0`; `pnpm -r --filter @stynx-web/angular-flow lint`, `typecheck`, `build`, and `test` (4 files / 37 tests); `pnpm i18n:check` (`angular-flow: 74 keys, 0 untranslated literals`); `git diff --check`. FE-F now follows the configured repo mutation threshold; orchestrator owns `FE-CLOSURE-REGISTRY.md` promotion. |
| FE-G G.10 stale per-package e2e smoke deletion/relocation | Removed stale package-local e2e smokes: `packages-web/angular-auth/test/e2e/angular-auth-consumer.e2e-spec.ts`, `packages-web/angular-flow/test/e2e/angular-flow-consumer.e2e-spec.ts`, `packages-web/angular-i18n/test/e2e/angular-i18n-consumer.e2e-spec.ts`, `packages-web/angular-profile/test/e2e/angular-profile-consumer.e2e-spec.ts`, `packages-web/angular-sessions/test/e2e/angular-sessions-consumer.e2e-spec.ts`, `packages-web/angular-storage/test/e2e/angular-storage-consumer.e2e-spec.ts`, `packages-web/angular-tenancy/test/e2e/angular-tenancy-consumer.e2e-spec.ts`, `packages-web/angular-trash/test/e2e/angular-trash-consumer.e2e-spec.ts`, `packages-web/angular-ui/test/e2e/angular-ui-consumer.e2e-spec.ts`, `packages-web/angular/test/e2e/angular-consumer.e2e-spec.ts`, `packages-web/sdk/test/e2e/sdk-consumer.e2e-spec.ts`. Kept no `packages-web/*/test/e2e/*.e2e-spec.ts` files; added no `test/unit/consumer-smoke.spec.ts` because the useful helper checks (`parseJwtPayload`, token storage, request IDs, tenancy providers, flow routes, storage executor, and component behavior) are already covered by package unit specs. | Not in scope for G.10; no Stryker thresholds or `scripts/test-matrix.config.json` policy edited. | `pnpm -r --filter './packages-web/*' test` passed for 13 package projects / 35 test files / 230 tests. `pnpm test:evidence` wrote `coverage/test-evidence.json` with `unit=41 integration=17 e2e=1 mutation=31 coverage=31 perf=1 smoke=1`. `pnpm test:matrix --no-color --compact` passed with all package-web rows `P` for unit and mutation where configured; `pnpm test:matrix --no-color --coverage` passed with every listed package green across lines/statements/branches/functions. | None. `git rm` could not create `.git/index.lock` in this sandbox, so deletion was applied as working-tree file removals; `git diff --check` passed after the report append. |
| FE-G G.1-G.5 package TestBed/support, route, and HTTP error fan-out | Added or audited `test/support/test-bed.ts` helpers and TestBed-rendered coverage across `@stynx-web/angular`, `angular-auth`, `angular-i18n`, `angular-profile`, `angular-sessions`, `angular-storage`, `angular-tenancy`, `angular-trash`, and `angular-ui`. Added package assertions for Angular empty states, auth login/logout/permission behavior, i18n locale switching, profile forms/security/routes including real route activation, sessions list behavior, storage upload/download error DOM, tenancy switching, trash selection/pagination, UI primitives/toasts, plus SDK network failure and abort-signal propagation. Repaired dependency boundaries by adding package-local `@angular/platform-browser` dev dependencies where browser TestBed helpers are imported (`angular-i18n`, `angular-profile`, `angular-sessions`, `angular-storage`, `angular-trash`). | G.9 policy review found no threshold edits required. Current mutation artifacts remain repo-policy green, including `@stynx-web/angular-flow` at configured threshold `60`; this row did not rerun all web Stryker jobs. | Parent reran the 10-package scoped gate: `pnpm -r --filter @stynx-web/angular --filter @stynx-web/angular-auth --filter @stynx-web/angular-i18n --filter @stynx-web/angular-profile --filter @stynx-web/angular-sessions --filter @stynx-web/angular-storage --filter @stynx-web/angular-tenancy --filter @stynx-web/angular-trash --filter @stynx-web/angular-ui --filter @stynx-web/sdk test`, `typecheck`, and `build`; all passed. The scoped test run covered 151 tests. Parent then reran `pnpm test:evidence` (`coverage/test-evidence.json` generated `2026-05-20T17:14:57.371Z` with `unit=41 integration=17 e2e=1 mutation=31 coverage=31 perf=1 smoke=1`), `pnpm test:matrix --no-color --compact`, `pnpm test:matrix --no-color --coverage`, and `git diff --check`; all passed. | None for package fan-out. `find packages-web -path '*/test/e2e/*.e2e-spec.ts' -print` returned no files, and the package-local browser-testing `@ts-expect-error` comments were removed after dependency repair. |
| `@stynx/reference-web` / FE-G G.6-G.8 Playwright fan-out, real-OIDC opt-in, and a11y report | Expanded `ownedCategorySpecs` to own the reference-web FE row categories (`auth`, `profile`, `sessions`, `iam`, `permissions`, `i18n`, `documents`, `records`, `trash`, `tenant`, `work-items`, `flows`, `audit`, `smoke`); added IAM users/roles/groups, permissions denial, and Flow design/runtime/analytics category specs; added shared IAM/Flow API route fixtures; converted legacy root specs to the shared fixture so a11y hooks apply. Added non-blocking `@axe-core/playwright` scan fixture with report reset/write path `reference/web/.test-results/a11y.json`. Added `PLAYWRIGHT_USE_REAL_OIDC=1` build/config branch with `scripts/serve-fake-oidc.mjs` and adapter support for local authorize/code/token flow while default mode remains dev-auth. | Not in scope for this reference-web slice. No Stryker thresholds or `scripts/test-matrix.config.json` policy edited. | `pnpm --filter @stynx/reference-web typecheck`, `build`, `test`, and `build:web` passed. `pnpm test:evidence` wrote `coverage/test-evidence.json` with `unit=41 integration=17 e2e=1 mutation=31 coverage=31 perf=1 smoke=1`; `pnpm test:matrix --no-color --compact` passed and shows `@stynx/reference-web` e2e `P`; `pnpm test:matrix --no-color --coverage` passed with all listed coverage rows green. Direct fake-OIDC validation passed: `/readyz` returned ok, `/authorize` returned a 302 with `code`/`state`/`tenantId`, and `/token` returned unsigned local JWT access/id tokens. | Local Playwright browser execution remains blocked by macOS sandboxing after installing Chromium into a workspace-local cache: both default scoped command `PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers pnpm exec playwright test --project=spa-only auth/login-success.spec.ts iam flows permissions --reporter=list` and real-OIDC scoped command `PLAYWRIGHT_USE_REAL_OIDC=1 PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers pnpm exec playwright test --project=spa-only auth/login-success.spec.ts --reporter=list` start the webServer stack, then fail at Chromium launch with `bootstrap_check_in org.chromium.Chromium.MachPortRendezvousServer... Permission denied (1100)`. Because no browser page opens, `a11y.json` is currently an empty report (`[]`) rather than populated scan results. |

Parent audit note (2026-05-20T16:50Z): the `@stynx/reference-web` G.6-G.8 row is accepted as implementation/configuration evidence, not as a current browser-run closure. Parent reran `pnpm --filter @stynx/reference-web typecheck`, `build`, `test`, and `build:web`; direct fake-OIDC `/readyz` -> `/authorize` -> `/token` validation passed; `pnpm test:evidence`, `pnpm test:matrix --no-color --compact`, `pnpm test:matrix --no-color --coverage`, and `git diff --check` passed. The `@stynx/reference-web` e2e `P` cell in `coverage/test-evidence.json` is still sourced from the older Playwright artifact ending `2026-05-19T10:58:54.411Z`; current local Playwright browser execution is blocked by `bootstrap_check_in ... Permission denied (1100)`, so the a11y report remains an empty `[]` until the browser host boundary is cleared. Parent normalized `pnpm-lock.yaml` back to the existing lockfile style; the retained delta is only the `@axe-core/playwright` dependency entries.

Current status: FE-G is in progress, not closed. IAM, audit, sessions, flow package mutation/test fan-out, package-wide TestBed/support and HTTP error fan-out (G.1-G.5), stale per-package e2e smoke deletion (G.10), and reference-web fan-out/configuration (G.6-G.8) now have current Auditor-verified rows. The `@stynx-web/angular-flow` FE-F mutation blocker is removed by the 2026-05-20 operator decision to use configured repository thresholds instead of the superseded ad hoc `>= 70 %` wave bar; flow passes the configured break threshold `60` in both the accepted `65.29492455418381` snapshot and the later `75.36 %` final score. The remaining FE-G closure condition is a current reference-web Playwright/a11y browser evidence rerun; the current matrix `e2e` pass is still sourced from the older artifact ending `2026-05-19T10:58:54.411Z`, while current local Chromium launch fails with `bootstrap_check_in ... Permission denied (1100)`.
