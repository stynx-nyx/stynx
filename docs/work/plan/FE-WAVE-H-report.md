# FE-WAVE-H Report

| Workstream | Deliverables | Validation | Status | Notes |
| ---------- | ------------ | ---------- | ------ | ----- |
| H.1 create-stynx-app starter | Added `tools/create-stynx-app/{bin.mjs,package.json,README.md}` and `tools/create-stynx-app/template/**`. The template is an Angular 20 standalone app using `provideStynxDefaults(...)`, `provideStynxAuth(...)`, tenant resolution, `provideStynxFlow(...)`, and `StynxSdkClient`. | Worker validation passed `node tools/create-stynx-app/bin.mjs --help`, `pnpm --filter @stynx-internal/create-stynx-app build`, a `/tmp/.../acme-portal --no-install` scaffold smoke, and `git diff --check -- tools/create-stynx-app`. Parent reran the same help/build/scaffold checks and verified `provideStynxDefaults|provideStynxAuth|provideStynxFlow` in the scaffold output. | CLOSED | Engineer-owned `tools/create-stynx-app/**`; no install was run during parent smoke, so validation covers scaffold correctness and CLI syntax, not a full fresh dependency install. |
| H.2 reference web docs | Rewrote `reference/web/README.md` with route-to-package demo surfaces and an explicit `Dev Auth Is Test-Only` section. | `rg -n "@stynx-web/angular-iam|@stynx-web/angular-audit|create-stynx-app|graph LR|dev-auth|pnpm start" packages-web/README.md reference/web/README.md packages-web/*/README.md` found `dev-auth` in `reference/web/README.md`; `git diff --check` passed. | CLOSED | Documents the local dev-auth bypass without changing runtime code. |
| H.3 per-package READMEs | Rewrote package README coverage for `packages-web/*`, including new `packages-web/angular-iam/README.md` and `packages-web/angular-audit/README.md`. | `find packages-web -maxdepth 2 -name README.md | sort` returned top-level plus all 13 package READMEs. `rg` found both `@stynx-web/angular-iam` and `@stynx-web/angular-audit` in the required docs. `git diff --check` passed. | CLOSED | README snippets were parent-checked against current `provideStynxDefaults`, IAM, audit, flow, and trash provider APIs. |
| H.4 packages-web overview | Rewrote `packages-web/README.md` with install matrix, manifest-grounded Mermaid `graph LR`, package index, and `tools/create-stynx-app` quickstart. | `rg` found `create-stynx-app`, `graph LR`, and `pnpm start`; parent corrected the quickstart from `pnpm dev` to the template's actual `pnpm start`. `git diff --check` passed. | CLOSED | The dependency diagram follows live package manifests rather than the initial sketch. |
| H.5 ADR consolidation | Added `ADR-FE-ICU-i18n-0002`, `ADR-FE-FLOW-PUBLISH-0003`, and `ADR-FE-AUDIT-CONTRACT-0004`; added a decision summary to `ADR-FE-PACKAGING-0001`; updated `docs/adr/README.md`. | `rg -n "ADR-FE-PACKAGING-0001|ADR-FE-ICU-i18n-0002|ADR-FE-FLOW-PUBLISH-0003|ADR-FE-AUDIT-CONTRACT-0004|MIGRATING" docs/adr packages-web/MIGRATING.md` found all required ADR IDs and index links. `git diff --check -- docs/adr packages-web/MIGRATING.md` passed. | CLOSED | Architect-owned ADR docs. |
| H.6 migration guide | Added `packages-web/MIGRATING.md` with FE-A through FE-G adopter-visible changes and before/after snippets. | Same ADR/migration `rg` check found `packages-web/MIGRATING.md`; full-tree `git diff --check` passed. | CLOSED | Documents APF imports, ICU catalogs, audit read contract, Flow publish contract, and FE-G test/a11y cleanup. |

## Parent Audit

Parent validation completed on 2026-05-20T19:49:38Z. Commands rerun by the Auditor: `node tools/create-stynx-app/bin.mjs --help`, `pnpm --filter @stynx-internal/create-stynx-app build`, `/tmp` scaffold smoke with `--no-install`, README inventory and required-term `rg` checks, ADR/migration `rg` checks, `pnpm test:matrix --no-color --compact`, `pnpm test:matrix --no-color --coverage`, scoped `git diff --check`, and full-tree `git diff --check`. All exited 0. FE-H is closed as docs/tooling work; no source test artifacts were regenerated in this wave.
