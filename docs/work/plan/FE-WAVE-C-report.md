# FE-WAVE-C Report

| Workstream | Commit | Validation excerpt | Notes |
| ---------- | ------ | ------------------ | ----- |
| C.1 ProfileService | Uncommitted workspace | `pnpm -r --filter @stynx-web/angular-profile build` -> built `@stynx-web/angular-profile` + `./testing`; `pnpm -r --filter @stynx-web/angular-profile test` -> 1 file / 2 tests passed; `pnpm --filter @stynx-web/angular-profile lint` -> 0 errors; `pnpm test:matrix --no-color --coverage` -> `@stynx-web/angular-profile` lines/statements/branches/functions all `✓✓`. | Added root-provided RxJS + signal-cached `ProfileService` with `reload`, `patch`, `setPreferences`, and avatar upload via `@stynx-web/angular-storage`; preserved FE-A packaging changes and did not promote FE-A. |
| C.2 StynxProfileFormComponent | Uncommitted workspace | `pnpm --filter @stynx-web/angular-profile lint` -> 0 errors; `pnpm -r --filter @stynx-web/angular-profile build` -> built `@stynx-web/angular-profile` + `./testing`; `pnpm -r --filter @stynx-web/angular-profile test` -> 1 file / 2 tests passed; `pnpm test:matrix --no-color --coverage` -> `@stynx-web/angular-profile` lines/statements/branches/functions all `✓✓`. | Rewrote `<stynx-profile-form>` as a typed non-null reactive form with signal status (`idle` / `saving` / `saved` / `error`), service-backed `ProfileService.patch(...)` submit, translated UI keys, `StynxToastService` success toast, and `ErrorBannerService` save failure banner; preserved the existing `name` / `email` / `locale` value/output contract for the C.2 Engineer slice. |
| C.3 StynxPreferencesFormComponent | Uncommitted workspace | `pnpm --filter @stynx-web/angular-profile lint` -> 0 errors; `pnpm -r --filter @stynx-web/angular-profile build` -> built `@stynx-web/angular-profile` + `./testing`; `pnpm -r --filter @stynx-web/angular-profile test` -> 1 file / 2 tests passed; `pnpm test:matrix --no-color --coverage` -> `@stynx-web/angular-profile` lines/statements/branches/functions all `✓✓`. All commands emitted the existing Node engine warning (`v26.0.0` vs `>=24 <25`). | Rewrote `<stynx-preferences-form>` as a typed non-null reactive form with signal status (`idle` / `saving` / `saved` / `error`), translated labels/validation/status/actions, `ProfileService.setPreferences(...)` submit, success toast, error banner, and locale propagation through `StynxI18nService.use(...)` when the saved locale changes; preserved the existing `locale` / `notifications` output contract for the C.3 Engineer slice. |
| C.4 Hosted auth action handoff components | Uncommitted workspace | `pnpm -r --filter @stynx-web/angular-auth build` -> built `@stynx-web/angular-auth` + `./testing`; `pnpm -r --filter @stynx-web/angular-auth test` -> 1 file / 18 tests passed; `pnpm -r --filter @stynx-web/angular-auth lint` -> 0 errors; `pnpm -r --filter @stynx-web/angular-profile build` -> built `@stynx-web/angular-profile` + `./testing`; `pnpm -r --filter @stynx-web/angular-profile test` -> 1 file / 5 tests passed; `pnpm -r --filter @stynx-web/angular-profile lint` -> 0 errors; `pnpm -r --filter @stynx-web/angular-sessions build` -> built `@stynx-web/angular-sessions` + `./testing`; `pnpm -r --filter @stynx-web/angular-sessions test` -> 1 file / 1 test passed; `pnpm -r --filter @stynx-web/angular-sessions lint` -> 0 errors. All commands emitted the existing Node engine warning (`v26.0.0` vs `>=24 <25`). `CI=true pnpm install --offline --no-frozen-lockfile` refreshed the workspace link/lock for the new profile -> auth dependency and exited 0, with a nonfatal husky `.git/config` lock warning during prepare. | Added the Architect-pinned hosted-action contract surface to `@stynx-web/angular-auth` (`hostedActions`, `getHostedActionLink`, `openHostedAction`) and exported `StynxChangePasswordHandoffComponent` / `StynxMfaEnrolmentHandoffComponent` from `@stynx-web/angular-profile`. The profile components are standalone + OnPush, render translated ready/unavailable/error states, use `STYNX_OIDC_ADAPTER`, and report invalid configured URLs through `ErrorBannerService` code `AUTH:CONFIG:hosted-action-url`; no provider-specific URL guessing was added. |
| C.5 profileRoutes + provideStynxProfile + UnsavedChangesGuard | Uncommitted workspace | `node -v` -> `v24.15.0`; `CI=true pnpm install --offline --no-frozen-lockfile` -> exit 0 with the existing nonfatal husky `.git/config` lock warning; `pnpm --filter @stynx-web/angular-profile lint` -> 0 errors; `pnpm -r --filter @stynx-web/angular-profile build` -> built `@stynx-web/angular-profile` + `./testing`; `pnpm -r --filter @stynx-web/angular-profile test` -> 1 file / 5 tests passed; `pnpm -r --filter @stynx-web/angular-sessions build` -> built `@stynx-web/angular-sessions` + `./testing`; `pnpm -r --filter @stynx-web/angular-sessions test` -> 1 file / 1 test passed; `pnpm lint` -> 35 tasks successful, exit 0; `pnpm test:matrix --no-color --coverage` -> every listed package `✓✓` for lines/statements/branches/functions. `pnpm i18n:check` confirmed `@stynx-web/angular-profile` has 36 keys and 0 untranslated literals, then failed on unrelated `@stynx-web/angular-audit` missing catalogs. | Added `STYNX_PROFILE_CLIENT`, `provideStynxProfile({ clientFactory })`, `PROFILE_ROUTES` / `profileRoutes()` for profile, preferences, and security child routes, a standalone security route host for the C.4 handoff components, and a generic `UnsavedChangesGuard` plus `beforeunload` registry. The profile and preferences forms now expose dirty-state guard hooks without changing their C.2/C.3 submit contracts. |
| C.6 SdkSessionsAdapter | Uncommitted workspace | Worker and parent audit reran `pnpm -r --filter @stynx-web/angular-sessions lint`, `typecheck`, `build`, and `test`; all exited 0. Parent test rerun passed 1 file / 1 test. | Added default `SdkSessionsAdapter`, `STYNX_SESSIONS_CLIENT`, `STYNX_SESSIONS_ADAPTER`, and `provideStynxSessions(...)`. Adapter calls `GET /auth/sessions`, `DELETE /auth/sessions/{id}`, and `POST /auth/sessions/revoke-others`; it derives the current session from `StynxSessionService` / JWT `sid`. |
| C.7 Sessions component polish | Uncommitted workspace | Same scoped sessions gates passed in worker and parent audit; root `pnpm lint`, `pnpm i18n:check`, `pnpm test:evidence`, `pnpm test:matrix --no-color --coverage`, and `git diff --check` all exited 0 at the 2026-05-20T04:41:57Z checkpoint. | `StynxActiveSessionsComponent` now supports provider default or input override, signal state, current-device badge, last IP / user-agent / last-seen display, revoke busy state, translated toast messages, and confirm dialog before revoke-others. |
| C.8 profile/sessions catalogs | Uncommitted workspace | `pnpm i18n:check` passed: `packages-web/angular-profile: 36 keys, 0 untranslated literals`; `packages-web/angular-sessions: 17 keys, 0 untranslated literals`. | Sessions catalogs were updated in `src/i18n/{en,pt-BR,keys}.json`; profile catalogs remain current. |
| C.9 package tests/mutation | Uncommitted workspace | FE-G worker and parent audit covered `@stynx-web/angular-sessions`: `pnpm -r --filter @stynx-web/angular-sessions lint`, `typecheck`, `build`, and `test` passed with 1 file / 13 tests; `pnpm --filter @stynx-web/angular-sessions stryker` passed with score `98.24561403508771` against break threshold `70`; refreshed `pnpm test:evidence` wrote `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z` with all aggregate levels passing; `pnpm test:matrix --no-color --coverage`, `pnpm i18n:check`, `pnpm lint`, and `git diff --check` all exited 0. | FE-G sessions adapter/provider/component fan-out added focused tests for `SdkSessionsAdapter`, `provideStynxSessions(...)`, tokens, provider/input adapter selection, session normalization/current-session derivation, and component load/revoke/revoke-others/busy/error behavior. |

Current status: FE-C is closed. C.1-C.8 were previously implemented and audited; C.9 is now covered by the FE-G sessions fan-out with sessions mutation at `98.24561403508771` against break threshold `70`, refreshed evidence at `2026-05-20T05:01:40.665Z`, and green parent gates (`pnpm test:evidence`, `pnpm test:matrix --no-color --coverage`, `pnpm i18n:check`, `pnpm lint`, `git diff --check`).
