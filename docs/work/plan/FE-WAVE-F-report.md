# FE-WAVE-F Report

| Workstream | Commit | Validation excerpt | Notes |
| ---------- | ------ | ------------------ | ----- |
| F.1 Empty / first-run states | pending local worker patch | `pnpm -r --filter @stynx-web/angular-flow test` → 3 files / 22 tests passed; `pnpm -r --filter @stynx-web/angular-flow build` → built main + testing entry points; `pnpm -r --filter @stynx-web/angular-flow lint` → exit 0; `pnpm test:matrix --no-color --coverage` → exit 0 with existing warnings (`@stynx-web/angular-flow` branches `!92`, `@stynx-web/angular-auth` branches `!97`, `@stynx/flow` lines/statements `!92`, branches `!33`, `@stynx-web/angular-iam` `0`). | Added `StynxFlowEmptyStateComponent`, wired designer zero-scope "Create your first scope" and active-scope zero-graph "Create your first graph" prompts, exported the component, and covered F.1 state branches. Version remains `0.1.0`; FE-A remains open under the root-lint waiver. |
| F.2 Publish / draft graph contract | pending local worker patch | `PATH="$PWD/node_modules/.bin:$PATH" pnpm -r --filter @stynx/flow test` → 9 files / 196 tests passed; `PATH="$PWD/node_modules/.bin:$PATH" pnpm -r --filter @stynx-web/angular-flow test` → 3 files / 26 tests passed; `pnpm -r --filter @stynx-web/angular-flow build` → built main + testing entry points; `pnpm -r --filter @stynx-web/angular-flow lint` → exit 0; `pnpm --filter @stynx/flow build` → exit 0; `pnpm --filter @stynx/flow lint` → exit 0; `pnpm test:matrix --no-color --coverage` → exit 0, every package row green. | Added `POST /flow/graphs/:id/publish` controller/service surface with `flow:publish:design`, audit action `flow.graph.publish`, idempotency header support, draft-version conflict handling, structural publish checks, and package-owned metadata-derived `status` / `publishedVersion` DTO fields. Added Angular publish DTOs, `FlowApiService.publishGraph`, translated designer badge/action UI, and F.2 unit coverage. The plain package test scripts needed the repo root bin path under local Node `26.0.0`; without that PATH prefix the recorder spawned no Vitest process and wrote `exitCode: null` with empty logs. `docs/api/openapi.json` currently has no `/flow/*` paths, so the source-of-truth confirmation for this slice is `docs/contracts/flow-api.md` + `docs/architecture/flow.md` + controller route tests. Version remains `0.1.0`; orchestrator owns the 1.0.0 bump. |
| F.3 My-tasks inbox | pending local worker patch | Node `v24.15.0`, pnpm `9.15.0`; `PATH="$PWD/packages-web/angular-flow/node_modules/.bin:$PWD/node_modules/.bin:$PATH" pnpm -r --filter @stynx-web/angular-flow test` → 3 files / 28 tests passed; same PATH `pnpm -r --filter @stynx-web/angular-flow build` → built main + testing entry points; `pnpm test:matrix --no-color --coverage` → exit 0, every package row green; `pnpm lint` → 35/35 tasks passed. | Added `StynxFlowMyTasksInboxComponent` under the existing flow task surface, exported it through the package barrel, mounted `/my-tasks`, rendered `StynxFlowTaskCardComponent` entries, refreshed via signal state, default 30 s polling with configurable `pollingIntervalMs`, and optional tenant-change refresh through `STYNX_FLOW_TENANT_CHANGED`. `FlowApiService.listTasks({ assignee: 'me', status: 'open' })` now maps to the backend-supported `mine=true` query while preserving direct assignee user-id filters. Added `flow.tasks.inbox.*` keys in `en` and `pt-BR`. Version remains `0.1.0`; orchestrator owns the 1.0.0 bump. |
| F.4 Per-run activity timeline | pending local worker patch | Worker and parent audit reran `pnpm -r --filter @stynx-web/angular-flow lint`, `typecheck`, `build`, and `test`; all exited 0. Parent unit rerun passed 3 files / 28 tests. Backend `pnpm --filter @stynx/flow lint`, `typecheck`, `build`, and `test` all exited 0; backend test passed 9 files / 197 tests. | Added `StynxFlowRunActivityComponent`, exported it, mounted `/runs/:runId/activity`, and added backend `GET /flow/runs/:id/activity` alias over paged run events. Remaining blocker: FE-G should add real router activation/guard coverage. |
| F.5 Richer fill question types | pending local worker patch | Same scoped gates passed; `pnpm i18n:check` passed with `packages-web/angular-flow: 74 keys, 0 untranslated literals`. | Fill editor now supports short/long text limits, file questions through `stynx-document-upload`, signature capture to PNG data URL, conditional reveal via `revealIf`/`visibleIf`, and ICU plural character counts. Remaining blocker: FE-G focused fill-type tests and mutation hardening. |
| F.6 Dashboard analytics | pending local worker patch | `@stynx-web/angular-flow` build/test passed; `@stynx/flow` build/test passed; `pnpm test:matrix --no-color --coverage` passed. | Added dashboard UI with open tasks, p50/p95 cycle time, 7/30-day completion rate, SLA breach count, frontend API method, and backend `GET /flow/analytics/dashboard`. Metric cards use local markup because `StynxMetricCardComponent` does not exist in the allowed FE-F edit scope. |
| F.7 OnPush uniform | pending local worker patch | Build and lint passed for `@stynx-web/angular-flow`; new components compile with `ChangeDetectionStrategy.OnPush`. | New run activity and dashboard components are standalone and OnPush; existing flow components already had OnPush. |
| F.8 Translation migration | pending local worker patch | `pnpm i18n:check` passed; `packages-web/angular-flow: 74 keys, 0 untranslated literals`. | Added English and pt-BR catalog keys for activity, dashboard, fill fallback, file, signature, and ICU plural labels. |
| F.9 Router tests | pending local worker patch | Existing route/export unit suite passed; package test passed 28/28. | Routes for activity and dashboard are mounted with permission guards. Remaining blocker: requested real Router navigation/guard denial spec belongs in FE-G test fan-out. |
| F.10 provideStynxFlow ergonomics / 1.0 readiness | pending local worker patch | `@stynx-web/angular-flow` typecheck/lint/build/test passed; lockfile diff for this slice is limited to the new angular-flow dependency on `@stynx-web/angular-storage`. | `provideStynxFlow` now accepts existing client instances or `{ clientFactory, tenantChanged$ }`; README includes SDK-backed, mock-backed, and custom-backed recipes; package version is `1.0.0`. |
| F.11 Tests and mutation | pending local worker patch | FE-G worker and parent audit covered `@stynx-web/angular-flow`: scoped lint, typecheck, build, and test passed with 4 files / 34 tests; `pnpm --filter @stynx-web/angular-flow stryker` passed the configured break threshold with refreshed evidence score `65.29492455418381` against break threshold `60`; `pnpm test:evidence` wrote `coverage/test-evidence.json` at `2026-05-20T05:01:40.665Z`; `pnpm test:matrix --no-color --coverage`, `pnpm i18n:check`, `pnpm lint`, and `git diff --check` all exited 0. | Accepted by operator decision on 2026-05-20: FE-F mutation acceptance now follows the repo-configured package threshold instead of the previous ad hoc `>= 70 %` wave bar. FE-G added real router activation/denial, dashboard stale-refresh, run activity paging/error/empty, fill file/signature/reveal, and provider ergonomics tests. |
| F.11 FE-G flow mutation closure | Uncommitted workspace | Node `v24.15.0`; `pnpm -r --filter @stynx-web/angular-flow lint` PASS; `typecheck` PASS; `build` PASS with existing ng-packagr export-condition warnings; `test` PASS (4 files / 37 tests); `pnpm --filter @stynx-web/angular-flow stryker` PASS with final mutation score `75.36` (`covered 76.48`, killed `569`, survived `175`, no coverage `11`, compile errors `295`; `.test-results` metric `76.47849462365592`); `pnpm test:evidence` wrote `coverage/test-evidence.json` at `2026-05-20T16:14:28.863Z` (`unit=41`, `integration=17`, `e2e=1`, `mutation=31`, `coverage=31`, `perf=1`, `smoke=1`); `pnpm test:matrix --no-color --coverage` PASS with every package row green; `pnpm i18n:check` PASS with `angular-flow: 74 keys, 0 untranslated literals`; `git diff --check` PASS. | Added extra mutation-hardening coverage without lowering package thresholds or changing not-applicable policy; this is above the configured repo threshold and is retained as useful coverage, but it is no longer required by a wave-local mutation bar. Files changed in this slice: `packages-web/angular-flow/test/components.spec.ts`, `test/flow-api.service.spec.ts`, `test/routes-and-exports.spec.ts`, and refreshed `packages-web/angular-flow/.test-results/{unit,mutation}.*`. Matrix impact: flow remains green across lines/statements/branches/functions and repo coverage matrix remains all green. Remaining work for this slice: none; orchestrator owns registry promotion. |

Current status: FE-F is closed by operator policy decision at this workspace checkpoint. F.1-F.10 are implemented with green scoped and global gates. F.11 now follows the configured repository threshold instead of the superseded ad hoc `>= 70 %` wave bar; `@stynx-web/angular-flow` has passed its configured package break threshold `60` in both the accepted `65.29492455418381` evidence snapshot and the later `75.36 %` final score (`76.47849462365592` evidence metric). The 2026-05-20 policy correction is recorded in `FE-WAVE-F-flow-installable.md`, `FE-WAVE-G-test-fan-out.md`, and `FE-05-testing-against-expectations.md`.
