# See ../../devai/CONSTITUTION.md

The canonical Constitution lives in the sibling DEVAI checkout. This pointer is generated by `devai init` v0.0.0 (Phase 21.D) so `devai doctor --adopter` can resolve the constitution without requiring the adopter to author one.
