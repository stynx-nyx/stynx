{
  "name": "stynx-db-tests",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node ./../../scripts/run-and-record.mjs --package stynx-db-tests --level unit --runner vitest -- vitest run --config ./vitest.config.ts",
    "test:db": "node ./../../scripts/run-and-record.mjs --package stynx-db-tests --level integration --runner vitest -- vitest run --config ./vitest.config.ts",
    "lint": "eslint --config ../../tools/eslint-config/test.mjs \"./**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
