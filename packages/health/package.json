{
  "name": "@stynx/health",
  "version": "1.0.0",
  "description": "Health and observability endpoints for stynx",
  "main": "dist/health/src/index.js",
  "types": "dist/health/src/index.d.ts",
  "engines": {
    "node": ">=24 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/health/src/index.d.ts",
      "require": "./dist/health/src/index.js",
      "default": "./dist/health/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint --config ../../tools/eslint-config/nest.mjs \"src/**/*.ts\"",
    "test": "node ./../../scripts/run-and-record.mjs --package @stynx/health --level unit --runner vitest -- vitest run --config ./vitest.config.ts",
    "test:int": "node ./../../scripts/run-and-record.mjs --package @stynx/health --level integration --runner vitest -- vitest run --config ./vitest.int.config.ts",
    "stryker": "node ./../../scripts/run-and-record.mjs --package @stynx/health --level mutation --runner stryker-vitest -- pnpm exec stryker run stryker.conf.mjs"
  },
  "dependencies": {
    "@nestjs/terminus": "^11.1.1",
    "@stynx/core": "workspace:*",
    "prom-client": "^15.1.3"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.1.19",
    "@nestjs/core": "^11.1.19",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^11.1.19",
    "@nestjs/testing": "^11.1.19",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
