{
  "name": "@stynx/privacy",
  "version": "1.0.0",
  "description": "Privacy exports, erasure orchestration, and ROPA generation for stynx",
  "main": "dist/privacy/src/index.js",
  "types": "dist/privacy/src/index.d.ts",
  "engines": {
    "node": ">=24 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/privacy/src/index.d.ts",
      "require": "./dist/privacy/src/index.js",
      "default": "./dist/privacy/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint --config ../../tools/eslint-config/nest.mjs \"src/**/*.ts\"",
    "test": "node ./../../scripts/run-and-record.mjs --package @stynx/privacy --level unit --runner vitest -- vitest run --config ./vitest.config.ts",
    "test:int": "node ./../../scripts/run-and-record.mjs --package @stynx/privacy --level integration --runner vitest -- vitest run --config ./vitest.int.config.ts",
    "stryker": "node ./../../scripts/run-and-record.mjs --package @stynx/privacy --level mutation --runner stryker-vitest -- pnpm exec stryker run stryker.conf.mjs",
    "test:e2e": "echo \"test:e2e removed during V6 cutover; convert to vitest if needed\" && false"
  },
  "dependencies": {
    "@stynx/core": "workspace:*",
    "@stynx/data": "workspace:*",
    "@stynx/storage": "workspace:*",
    "jszip": "^3.10.1",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.1.19",
    "@nestjs/core": "^11.1.19",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^11.1.19",
    "@nestjs/testing": "^11.1.19",
    "@stynx/testing": "workspace:*",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
