{
  "name": "@stynx/logging",
  "version": "1.0.0",
  "description": "Structured logging primitives for stynx",
  "main": "dist/logging/src/index.js",
  "types": "dist/logging/src/index.d.ts",
  "engines": {
    "node": ">=24 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/logging/src/index.d.ts",
      "require": "./dist/logging/src/index.js",
      "default": "./dist/logging/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint --config ../../tools/eslint-config/nest.mjs \"src/**/*.ts\"",
    "test": "node ./../../scripts/run-and-record.mjs --package @stynx/logging --level unit --runner vitest -- vitest run --config ./vitest.config.ts",
    "test:int": "node ./../../scripts/run-and-record.mjs --package @stynx/logging --level integration --runner vitest -- vitest run --config ./vitest.int.config.ts",
    "stryker": "node ./../../scripts/run-and-record.mjs --package @stynx/logging --level mutation --runner stryker-vitest -- pnpm exec stryker run stryker.conf.mjs"
  },
  "dependencies": {
    "@stynx/core": "workspace:*",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.1.19",
    "@nestjs/core": "^11.1.19",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^11.1.19",
    "@nestjs/testing": "^11.1.19",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
