name: Ephemeral Environment

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read

jobs:
  stub:
    name: ephemeral-env-stub
    runs-on: ubuntu-latest
    steps:
      - name: Report placeholder environment target
        run: |
          echo "TODO: provision CDK ephemeral environment"
          echo "target env=ephemeral-${{ github.event.pull_request.number }}"
