## <prompt:rationalization-cleanup>

description: |
Execute the rationalization cleanup plan produced by the Golden Falcon
inspection (docs/work/rationalization/). Removes agent-prompt residue,
fixes config drift, and tidies documentation without changing any
observable behavior or published package surface.
wave: "1-4"
catalog: "docs/work/rationalization/02-CLEANUP-CATALOG.md"
execution-plan: "docs/work/rationalization/03-EXECUTION-PLAN.md"

---

ROLE
You are a repository cleanup agent operating on the STYNX monorepo. You
execute the wave-ordered steps defined in the rationalization plan without
introducing behavioral changes, touching published package surfaces, or
modifying anything under `specs/`.

CONSTRAINTS

- Do NOT modify any file under `specs/`. Those are load-bearing per the
  spec corpus (STYNX-SPEC-v0.6, ADRs, GAP docs).
- Do NOT modify any `packages/*/src/index.ts` exported surface.
- Each wave must fully pass verification before starting the next wave.
- Use conventional commits: `chore(clean): …` for code/config cleanup,
  `docs(clean): …` for doc cleanup. Include catalog IDs in the commit body.
- Do NOT amend commits. One commit per step.

---

PRE-FLIGHT

1. Confirm the repo is on branch `main` with a clean working tree:
   `git status --short` must be empty.

2. Read the full catalog before starting:
   `cat docs/work/rationalization/02-CLEANUP-CATALOG.md`

3. Read AGENTS.md and obey its rules. Note: AGENTS.md rule 6 ("study
   SUMMARY.md … coordinate through TODO.md") refers to files that CLEAN-001
   and CLEAN-005 will act on. The replacements for those coordination
   functions are:
   - `SUMMARY.md` → replaced by git log (`git log --oneline -20`).
   - `TODO.md` → open items are being triaged as part of this prompt (see
     CLEAN-005 below).

4. Run the baseline check:
   `pnpm lint:deadcode && pnpm lint:deps`
   Both must report 0 issues before any changes land.

---

WAVE 1 — Pure deletions
Branch: `clean/remove-agent-prompt-residue`
Covers: CLEAN-001, CLEAN-002, CLEAN-003, CLEAN-010

STEP 1.1 — Delete agent-prompt artifacts (CLEAN-001)

- `git rm PROMPT_UPDATE_FRONTEND_ENV_WITH_PATCHES.md SUMMARY.md`
- Grep for stale references:
  `git grep -lF 'PROMPT_UPDATE_FRONTEND_ENV_WITH_PATCHES' -- '*.md' '*.yml' '*.json'`
  `git grep -lF 'SUMMARY.md' -- '*.md' '*.yml' '*.json'`
  Update any references found (likely none, but verify).

STEP 1.2 — Delete applied patch mailbox (CLEAN-002)

- `git rm patches/*.patch`
- `git rm -r patches/` (or `git rm patches/` if now empty)
- Grep for patch-dir references:
  `git grep -rF 'patches/' -- '*.md' '*.yml' '*.sh' '*.json'`
  Update any stale references found.

STEP 1.3 — Delete vestigial placeholder (CLEAN-003)

- `git rm src/.gitkeep`
- If `src/` is now empty: `git rm -r src/` (empty dirs don't track in git,
  but the gitkeep was the only file, so the dir itself disappears).

STEP 1.4 — Gitignore generated changeset status (CLEAN-010)

- Append `/.changeset/status.json` to `.gitignore` (add under the existing
  "generated artifacts" section, or create such a section if absent).
- `git rm --cached .changeset/status.json`
- Verify: `pnpm release:status` still runs and generates the file (it
  should land as untracked+ignored after the `git rm --cached`).

COMMIT wave 1:
`git add -u .gitignore && git commit -m "chore(clean): remove agent-prompt residue and gitignore changeset status

CLEAN-001 CLEAN-002 CLEAN-003 CLEAN-010

- Remove PROMPT_UPDATE_FRONTEND_ENV_WITH_PATCHES.md (248-line agentic
  prompt artifact from a one-shot patch series, already merged)
- Remove SUMMARY.md (frozen commit log from the same series)
- Remove patches/\*.patch (15 git-format-patch mailbox files, applied)
- Remove src/.gitkeep (vestigial empty placeholder)
- Gitignore .changeset/status.json (generated by pnpm release:status;
  tracking it causes merge-conflict churn)"`

WAVE 1 VERIFICATION:

- `pnpm -r build 2>&1 | tail -5` — must succeed.
- `pnpm -r typecheck 2>&1 | tail -5` — must succeed.
- `pnpm -r lint 2>&1 | tail -5` — must succeed.
- `pnpm lint:deadcode` — must still report 0 issues.
- `pnpm lint:deps` — must still report 0 issues.
- `git status --short` — only shows `.changeset/status.json` as
  untracked/ignored (i.e., not listed), nothing else dirty.

---

WAVE 2 — Internal consolidations
Branch: `clean/config-consolidation`
Covers: CLEAN-006, CLEAN-008, CLEAN-010 (already done), CLEAN-013

STEP 2.1 — Consolidate root tsconfigs (CLEAN-006)
Current state:
tsconfig.json (2 lines):
{"extends": "./tools/tsconfig/base.json"}
tsconfig.base.json (8 lines):
{"extends": "./tools/tsconfig/base.json",
"compilerOptions": {"noImplicitOverride": true,
"experimentalDecorators": true,
"emitDecoratorMetadata": true}}

Recommended approach:
a. Open `tools/tsconfig/base.json` and add the three compilerOptions from
`tsconfig.base.json` IF they are not already present. These options
are needed by NestJS (decorators) and the full monorepo; adding them
to the shared base is correct.
b. `git rm tsconfig.json tsconfig.base.json`
c. `git grep -rn 'tsconfig\.base\.json\|"\./tsconfig"' -- '*.json' '*.ts' '*.mts' '*.yml' '*.md'`
Update any tooling references that resolve to the deleted files
(VS Code settings, jest rootDir, etc.). If none found, proceed.
d. Verify Angular workspaces still compile (Angular Language Service may
read the root tsconfig):
`pnpm --filter @stynx-web/angular build`
`pnpm --filter @stech/reference-frontend build`

STEP 2.2 — Wire infra/cdk tsconfig to shared preset (CLEAN-008)
a. Read `infra/cdk/tsconfig.json` fully.
b. Compare its compilerOptions against `tools/tsconfig/node24.json`.
c. If divergences are intentional (e.g., CDK requires `target: ES2020`),
keep them as overrides. Otherwise drop them.
d. Add `"extends": "../../tools/tsconfig/node24.json"` as the first key.
e. `pnpm --filter @stynx-infra/cdk build` (if the script exists).

STEP 2.3 — Re-validate knip ignore-list (CLEAN-013)
a. For each file in `knip.config.ts` `ignoreFiles`, confirm the file
still exists:
`for f in <each ignoreFiles entry>; do ls "$f" 2>/dev/null || echo "MISSING: $f"; done`
Remove any entries whose file no longer exists.
b. For the six `packages/stynx-backend/src/{idempotency,rate-limit}/` entries,
read `packages/stynx-backend/src/index.ts` and check whether any of the
six modules (`constants.ts`, `*.module.ts`, `pg-*.store.ts`) are already
re-exported. If not, attempt to add re-exports and remove the corresponding
`ignoreFiles` entries.
c. Run `pnpm lint:deadcode` — must still report 0 issues. If it reports new
issues after removing an ignoreFiles entry, either revert that entry or
fix the underlying dead symbol.

COMMIT wave 2:
`git commit -m "chore(clean): consolidate root tsconfigs and re-validate knip ignores

CLEAN-006 CLEAN-008 CLEAN-013

- Merge noImplicitOverride + decorator flags into tools/tsconfig/base.json
- Remove redundant root tsconfig.json and tsconfig.base.json
- Wire infra/cdk/tsconfig.json to extend tools/tsconfig/node24.json
- Re-validate knip ignoreFiles list; remove stale entries if any"`

WAVE 2 VERIFICATION:

- `pnpm -r build` — must succeed.
- `pnpm -r typecheck` — must succeed.
- `pnpm lint:deadcode` — must still report 0 issues.
- `pnpm -r test --silent` — must succeed.

---

WAVE 3 — Convention alignment
Branch: `clean/convention-alignment`
Covers: CLEAN-007, CLEAN-011, CLEAN-017

STEP 3.1 — Document pnpm.overrides pins (CLEAN-007)
a. For each pin in `package.json` `pnpm.overrides`, find the commit that
introduced it:
`git log --all -p -- package.json | grep -A5 -B5 '<pkg-name>'`
Surface the rationale (CVE? broken transitive? perf?).

Pins to investigate:

- `@types/node@25.6.0`
- `handlebars@4.7.9`
- `postcss@8.5.12`
- `serialize-javascript@7.0.5`
- `webpackbar@7.0.0`

b. Create `OVERRIDES.md` at the repo root with one section per pin:

```markdown
# pnpm Overrides

Entries in `package.json > pnpm.overrides` that need justification
and a removal condition.

## @types/node — pinned to X.Y.Z

**Reason:** <filled in from git log>
**Remove when:** <condition, e.g., "Angular CLI 20 lifts the upper bound">

## handlebars — pinned to X.Y.Z

**Reason:** CVE-2021-23369 / CVE-2021-23383 (prototype pollution)
**Remove when:** Transitive consumers upgrade beyond the vulnerable range

[... repeat for each pin ...]
```

c. If the git log reveals any pin that no longer applies (upstream fixed
the issue in the currently-installed transitive range):

- Remove it from `package.json`.
- `pnpm install --frozen-lockfile=false`
- `pnpm -r build && pnpm -r test`
- Verify clean before committing.

STEP 3.2 — Rename mis-named root script (CLEAN-011)
a. In root `package.json`, rename `"bootstrap:typecheck"` → `"bootstrap:build"`.
b. `git grep -rn 'bootstrap:typecheck' -- '*.yml' '*.md' '*.sh' '*.json'`
Update any callers.

STEP 3.3 — Verify .DS_Store gitignore coverage (CLEAN-017)
a. `cat .gitignore | grep -i '\.DS_Store'`
b. If the root `.gitignore` covers `**/.DS_Store` or `.DS_Store`, no action.
c. If only the root `.DS_Store` is covered but not nested ones, add `**/.DS_Store`.
d. Verify: `git ls-files --others --ignored --exclude-standard | grep DS_Store`
should return the working-tree `.DS_Store` (confirming it's excluded, not
hidden from git entirely).

COMMIT wave 3:
`git commit -m "chore(clean): document overrides pins, rename script, verify gitignore

CLEAN-007 CLEAN-011 CLEAN-017

- Add OVERRIDES.md documenting each pnpm.overrides pin's rationale and
  removal condition
- Rename bootstrap:typecheck → bootstrap:build (script runs build, not typecheck)
- Ensure .gitignore covers \*\*/.DS_Store at all depths"`

WAVE 3 VERIFICATION:

- `pnpm bootstrap:build` — must succeed.
- `pnpm -r build && pnpm -r lint` — must succeed.
- `pnpm lint:deadcode && pnpm lint:deps` — must report 0 issues.

---

WAVE 4 — Documentation pass
Branch: `clean/doc-pass`
Covers: CLEAN-004, CLEAN-005, CLEAN-009, CLEAN-012, CLEAN-015, CLEAN-016

STEP 4.1 — Resolve .release/drafts/ disposition (CLEAN-009)
a. Read `scripts/generate-release-drafts.mjs` to confirm whether it
regenerates the files in `.release/drafts/`.
b. If GENERATED: add `/.release/drafts/*.md` to `.gitignore` and
`git rm --cached .release/drafts/*.md` (keep the README).
Add a header to `.release/drafts/README.md`:
"These files are generated by `pnpm release:drafts`. Do not edit manually."
c. If HAND-AUTHORED: add a header to `.release/drafts/README.md`:
"These are hand-authored release-note seeds. Run `pnpm release:drafts`
to regenerate from the template; then edit for human quality."
d. Verify: `pnpm release:drafts` runs cleanly.

STEP 4.2 — Expand or redirect GOVERNANCE.md (CLEAN-004)
a. `ls docs/governance/` — check whether governance content already lives
there (it should per `.codex/system.md`).
b. Replace the 3-line stub `GOVERNANCE.md` with either:

- A redirect: "See [docs/governance/](docs/governance/) for governance
  policies, audit trails, and compliance scorecards."
- Or a brief introduction (5–10 lines) linking to the relevant subdirs.
  c. Do NOT move files under `docs/governance/`; just update the stub pointer.

STEP 4.3 — Triage TODO.md (CLEAN-005)
a. Read `TODO.md` in full.
b. For each open checkbox, decide based on current repo state:

- If the work is already done (check CI, recent git log, and the
  corresponding doc/spec): mark it `[x]` with a note.
- If the work is genuinely still open and the prompt-numbered workflow
  is active: leave it open; add a header note:
  "Open items from the prompt-numbered workflow. Each references the
  corresponding prompt session. For current CI status, see GitHub Actions."
- If the prompt-numbered workflow has been retired and these are now
  plain GitHub Issues: add a note pointing to the issue tracker and
  mark the file as superseded. Optionally delete it and redirect to
  `docs/work/` if the team confirms.
  c. Do NOT delete TODO.md without explicitly confirming the prompt-numbered
  workflow is retired.

STEP 4.4 — Expand 3-line stub READMEs (CLEAN-012)
For each of these files that is currently 3 lines (placeholder-only):
packages-web/angular-trash/README.md
packages-web/angular-storage/README.md
packages-web/angular-sessions/README.md
packages-web/angular-profile/README.md
packages-web/angular-i18n/README.md

Expand each to ~8–12 lines following this template:

````markdown
# @stynx-web/<pkg-name>

Angular integration module for STYNX <concept>. Provides [one sentence
describing what it does and who uses it].

## Usage

Import `<MainModule>` in your Angular application module:

```typescript
import { <MainModule> } from '@stynx-web/<pkg-name>';
```
````

See the [STYNX documentation](../../docs/) for full API reference.

```

Read the package's `src/index.ts` or `src/public-api.ts` to discover
the exported symbols before writing the usage line.

STEP 4.5 — Reconcile AGENTS.md with .codex/ (CLEAN-016)
a. Read `AGENTS.md`, `.codex/README.md`, `.codex/system.md`.
b. Identify any content that is duplicated between the files.
c. Add a header to `AGENTS.md` distinguishing its scope:
   "This file is the `agents.md` protocol surface (https://agents.md).
   For repo-specific agent configuration, see `.codex/`."
d. If `.codex/system.md` or `.codex/README.md` duplicates AGENTS.md rules,
   replace the duplicated text with a cross-reference.

STEP 4.6 — Clarify audit/ directory purpose (CLEAN-015)
a. `ls docs/work/inv/` — it exists but is empty (the rationalization
   outputs went to `docs/work/rationalization/`).
b. Check whether `audit/REPO-GAP-ANALYSIS.md` should move under
   `docs/work/inv/` (the apparent intended location based on the dir
   name and `.codex/system.md`'s rule "keep scratch artifacts under
   docs/work/**").
c. If the team's convention is that `audit/` is the standing location for
   audit-stream output: add a `audit/README.md` explaining this.
d. If `audit/` was a one-shot artifact location and the canonical location
   is `docs/work/inv/`: `git mv audit/REPO-GAP-ANALYSIS.md docs/work/inv/`
   and add `docs/work/inv/README.md` explaining the directory's purpose.
e. Do NOT change the content of `REPO-GAP-ANALYSIS.md`.

COMMIT wave 4:
`git commit -m "docs(clean): documentation pass — governance, TODO, stubs, audit dir

CLEAN-004 CLEAN-005 CLEAN-009 CLEAN-012 CLEAN-015 CLEAN-016

- Resolve .release/drafts/ disposition (generated vs hand-authored)
- Expand GOVERNANCE.md stub to redirect to docs/governance/
- Triage TODO.md open items (mark completed, note active work)
- Expand 5 placeholder angular-* READMEs to functional stubs
- Distinguish AGENTS.md (protocol surface) from .codex/ (repo config)
- Clarify audit/ directory purpose or relocate to docs/work/inv/"`

WAVE 4 VERIFICATION:
- `pnpm lint:deadcode` — 0 issues.
- `pnpm lint:deps` — 0 issues.
- `pnpm -r build && pnpm -r lint` — must succeed.
- Visual: `cat GOVERNANCE.md` reads sensibly.
- Visual: `cat packages-web/angular-trash/README.md` contains a meaningful
  one-paragraph description.

---

FINAL GATE

After all four waves:
1. `git diff --stat origin/main` — should list only the files touched by
   the cleanup (no source files, no spec files, no packages/*/src/index.ts).
2. `pnpm lint:deadcode` — 0 issues.
3. `pnpm lint:deps` — 0 issues.
4. `pnpm -r build` — succeeds.
5. `pnpm -r typecheck` — succeeds.
6. `pnpm -r lint` — succeeds.
7. `pnpm -r test --silent` — succeeds.

If all gates pass, open a PR per wave branch. Each PR title should follow
the pattern: `chore(clean): <wave description> (CLEAN-NNN, ...)`.

OUT-OF-SCOPE ITEMS (do not act on in this prompt)
- CLEAN-014 (logging callsite sweep) — requires a scan; handle separately.
- CLEAN-018 (dependabot backlog) — process item for the maintainer.
- STRUCT-001 (`@stynx/*` vs `@stech/*` namespace) — RFC required.
- STRUCT-002 (angular-* package consolidation) — RFC required.
- Anything under `specs/` — load-bearing, do not touch.
</prompt:rationalization-cleanup>
```
