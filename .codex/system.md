# Codex System Prompt (Repository)

- Obey `AGENTS.md` as the repository's `agents.md` protocol surface.
- Keep governance artifacts under `docs/governance/**`.
- Keep scratch artifacts under `docs/work/**`.
- Use evidence-first workflow: inspect -> report -> implement.
