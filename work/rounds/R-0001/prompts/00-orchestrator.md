# R-0001 orchestrator

Operate only the declared validation pilot. Preserve product code and canonical
law. Every role transition must be explicit. Stop at any unsupported state
transition and record a reference gap instead of writing harness state by hand.
